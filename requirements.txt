# 仅允许在远程环境执行安装，例如 `pip install -r requirements.txt`
# 核心（必须匹配）
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# HF 生态
transformers>=4.52.3
tokenizers<=0.22.1
accelerate>=0.30.1
trl>=0.9.3
datasets>=2.20.0
evaluate>=0.4.2
huggingface_hub>=0.24.6
peft>=0.11.1

# 量化（可选）：如需 4/8bit 再单独安装适配 CUDA 的 bitsandbytes 版本
# 留空，避免在不需要量化时引入不兼容的 CUDA 依赖

# 其他
numpy==2.1.2
scipy>=1.14.1
scikit-learn>=1.5.2
pyyaml>=6.0.2
jinja2>=3.1.4
rich>=13.9.2
lm-eval==0.4.9.1