# 注：需在远程执行 `source scripts/setup_env.sh --dry-run false` 之前根据需要修改
HF_ENDPOINT=https://hf-mirror.com
HF_HOME=/path/to/project/.cache/huggingface
DATA_ROOT=/path/to/project/data_raw
PROC_ROOT=/path/to/project/data_proc
TOKENIZERS_PARALLELISM=false
